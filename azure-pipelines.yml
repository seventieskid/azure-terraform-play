trigger: none

jobs:
- job: terraform_test
  workspace:
    clean: all
  pool:
    vmImage: 'ubuntu-latest'
  steps:
  - task: Docker@2
    displayName: Login to ACR
    inputs:
      command: login
      containerRegistry: azuron-sc-workload-identity
  - script: |
      docker run azuron.azurecr.io/bash 'ls -aRl /home'
